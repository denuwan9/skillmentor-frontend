export interface CardElement {
  title: string;
  rating: number;
  reviews: number;
  name: string;
  role: string;
  company: string;
  tutorSince: string;
  description: string;
  enrollments: number;
  isCertified: boolean;
  image: string; // URL for avatar
  badge: string; // URL for the badge image
}
